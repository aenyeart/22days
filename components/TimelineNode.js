import { View } from "react-native";

export default () => {

  return (
    <View style={{
      width: 11,
      height: 11,
      backgroundColor: '#D5FF2C',
      position: 'absolute',
      left: -22,
      top: 5,
      borderRadius: 50,
      zIndex: 1,

    }} />
  );

}