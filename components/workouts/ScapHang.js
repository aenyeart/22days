import { Text } from 'react-native';
import Input from '../Input.js';

export default ScapHang = () => {
  return <>
    <Text>Scap-Pull Hang</Text>
    <Input placeholder="Enter scap-pull hang duration (seconds)" actionType="SET_CURRENT_SCAP_HANG" />
  </>;
}
